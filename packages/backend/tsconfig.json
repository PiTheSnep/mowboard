{
	"extends": "../../tsconfig.json",

	"compilerOptions": {
		"outDir": "dist",

		"baseUrl": "./src",
		"paths": {
			"~/*": ["src/*"]
		}
	},

	"references": [{ "path": "../shared" }],

	"include": ["src/**/*.ts"]
}
