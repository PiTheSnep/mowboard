import * as React from 'react';

import { Form } from 'antd';
import { FullHeight } from '~/components';

export const GuildSettings = (): JSX.Element => (
  <FullHeight>
    <FullHeight.Content>
      <Form></Form>
    </FullHeight.Content>
  </FullHeight>
);
